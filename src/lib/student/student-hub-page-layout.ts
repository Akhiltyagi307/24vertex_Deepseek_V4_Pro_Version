/**
 * Centered main column for student hub routes (profile/settings, practice, subscription).
 * ~80% of the sidebar inset on md+; full width on small screens.
 */
export const studentHubPageShellClassName = "mx-auto w-full min-w-0 md:max-w-[80%]";
