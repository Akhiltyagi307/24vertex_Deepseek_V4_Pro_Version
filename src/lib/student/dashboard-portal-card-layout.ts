/** Shared layout tokens for dashboard assignments + leaderboard cards. */
export const dashboardPortalCardClassName = "flex h-full flex-col py-5 shadow-none";

/** Applied when the card has list content so paired cards stay aligned. */
export const dashboardPortalCardPopulatedClassName =
	"min-h-[320px] medium:min-h-[360px]";

export const dashboardPortalCardContentClassName = "flex min-h-0 flex-1 flex-col px-5 pb-3 pt-0";

export const dashboardPortalCardEmptyClassName =
	"flex flex-col items-center justify-center gap-4 rounded-xl bg-muted/15 px-5 py-8 text-center";
