/** Primary CTA for `/signup/role-picker` across landing (header, hero, pricing, sheet). */
export const LANDING_ROLE_SIGNUP_PRIMARY_CTA = "Start 14-day trial";

/** Same Tailwind classes as the hero secondary pill (e.g. "Explore features first" in `AcmeHero`). */
export const LANDING_MARKETING_SECONDARY_CTA_BUTTON_CLASSNAME =
	"h-10 rounded-full px-6 text-sm font-semibold shadow-none";

/** Same gap as the hero primary / secondary row: `gap-3` when stacked, `medium:gap-4` when in a row. */
export const LANDING_MARKETING_HERO_CTA_ROW_GAP_CLASSNAME = "gap-3 medium:gap-4";
