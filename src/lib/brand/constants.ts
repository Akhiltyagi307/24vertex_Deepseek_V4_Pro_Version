/** User-facing product name (display copy, emails, PDFs, metadata). */
export const PRODUCT_NAME = "24Vertex";

/** Technical prefix for keys, cookies, GUCs, JWT issuer (no leading digit). */
export const PRODUCT_SLUG = "vertex24";

/** Lowercase form for alt text, download filenames, etc. */
export const PRODUCT_NAME_LOWER = "24vertex";

/** Legacy technical slug — read-only shims during transition. */
export const LEGACY_PRODUCT_SLUG = "eduai";
