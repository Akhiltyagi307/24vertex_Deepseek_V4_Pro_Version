import "katex/dist/katex.min.css";

/** Wraps student routes that render LaTeX (practice, QnA, doubt-chat, report detail). */
export default function KatexRouteLayout({ children }: { children: React.ReactNode }) {
	return children;
}
